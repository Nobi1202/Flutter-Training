import 'package:flutter/cupertino.dart';

class CustumButton extends  StatelessWidget {
  const CustumButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:
    );
  }
}